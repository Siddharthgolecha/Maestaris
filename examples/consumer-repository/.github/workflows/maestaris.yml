name: Maestaris protocol

on:
  issues:
    types: [opened, edited, reopened, closed]
  issue_comment:
    types: [created, edited, deleted]

permissions:
  contents: read
  issues: write

jobs:
  maestaris:
    uses: Siddharthgolecha/Maestaris/.github/workflows/reusable-maestaris.yml@v0.6.1
    with:
      maestaris_ref: v0.6.1
