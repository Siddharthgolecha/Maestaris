# Security

Do not place API keys, tokens, credentials, private prompts, or sensitive datasets in mailbox comments, configuration files, commits, or examples.

Use your platform's secret store for credentials. Treat public mailbox PRs as public text. Workers should receive only the permissions required for their assigned work.

For a private security issue, contact the repository maintainer through an appropriate private channel rather than opening a public issue.
